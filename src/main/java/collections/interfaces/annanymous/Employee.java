package collections.interfaces.annanymous;

public interface Employee {

    // with one abstract class we use lambda expression
    public String getSalary();

    //with more than 1 abstract method we use annyonomous inner class
    public int getEmpCode();
}
